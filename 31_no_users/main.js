//question no 31
var userNames = ["zaheer", "zakheer", "asad", "subhan", "ali"];
if (userNames.length === 0) {
    console.log("we need to find some users!");
}
else {
    userNames = [];
    console.log("All users names have been removed. " + userNames.length);
}
